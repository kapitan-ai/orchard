#!/bin/sh
set -eu
# Placeholder managed Postgres wrapper.
# Packaging will eventually dispatch to the managed Postgres container helper.
echo "orchard-managed-postgres is not implemented yet (M0 scaffold)." >&2
exit 1
