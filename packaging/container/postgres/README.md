# postgres container notes

Intended future responsibilities:
- local loopback-only Postgres runtime
- persistent data under `/Library/Application Support/Orchard/data/`
- health checks via `pg_isready`
