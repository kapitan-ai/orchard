# Goal Template

These templates are examples for local ignored `goals/<slug>/` packages.
Copy them into an ignored local goal directory when useful.

Templates are tracked so contributors understand the policy. Filled-in goal
packages remain local unless their conclusions are rewritten into durable
product artifacts.
