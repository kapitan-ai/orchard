## 1. Contract

- [x] 1.1 Define the exact Phase 0 config, grammar-backed safe result, buffered
      terminal, cross-plane reconciliation, launcher, and pin requirements.
- [x] 1.2 Record explicit deferrals for incremental streaming,
      Collector/Grafana/#123, database-identity correlation, crash
      normalization, and pin preflight.

## 2. Probe artifact

- [x] 2.1 Add the shell entrypoint and versioned Elixir probe implementation.
- [x] 2.2 Add the exact version 1 example configuration with environment-only
      model and credential values.
- [x] 2.3 Enforce canonical identifiers and fail-closed buffered terminal
      candidates with producer-legal statuses.
- [x] 2.4 Reconcile Controller-local HTTP and durable terminal outcomes.
- [x] 2.5 Restrict credential delivery to loopback HTTP or explicitly verified
      system-CA HTTPS, with distinct and safe resolved values.
- [x] 2.6 Preserve caller-relative config paths and isolate compilation output
      from result stdout.

## 3. Safety and tests

- [x] 3.1 Add hostile configuration, identifier, and result-schema coverage.
- [x] 3.2 Add malformed, contradictory, colonless/repeated-field, duplicate,
      scalar, and invalid-UTF-8 terminal coverage.
- [x] 3.3 Add persisted durable-invariant, pure cross-plane mapping, and
      sanitized ordinary Repo-query failure coverage without claiming
      endpoint-to-Repo acceptance coverage.
- [x] 3.4 Add explicit TLS-option construction, resolved-value, existing and
      nonexistent caller-relative path, invalid-config JSON stdout, and exits
      `1`/`2`/`64` coverage. Add response media-type, lexical raw-authority, and
      out-of-contract HTTP-status coverage. Live TLS conformance and subprocess
      exit `0` are explicitly deferred.

## 4. Pilot consumption and validation

- [x] 4.1 Add the issue #118 CP1 pin example and producer/consumer ownership
      documentation.
- [x] 4.2 Add operator guidance for trust policy, no-echo credential entry,
      buffered evidence strength, path handling, and exit codes.
- [x] 4.3 Run strict OpenSpec validation and the complete applicable Elixir
      quality workflow for handoff.
